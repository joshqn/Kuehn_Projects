<%= render partial: 'type_switcher' %>

<% @projects.each_with_index do |val, index | %>

  <% if current_user && current_user.id == 1 %>

    <div class="row">
      <div class="container">
        <button class="btn btn-default" style="margin-top: 5px;">Edit</button>
      </div>
    </div>

  <% end %>


  <% if index % 2 == 0 %>

    <%= render partial: 'leftSide_Project', locals: { val: val } %>

  <% else %>

    <%= render partial: 'rightSide_Project', locals: { val: val } %>

  <% end %>
<% end %>

<%= will_paginate %>





<!-- <p id="notice"><%= notice %></p>

<h1>Projects</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Projtype</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.title %></td>
        <td><%= project.description %></td>
        <td><%= project.projType %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project', new_project_path %> -->
